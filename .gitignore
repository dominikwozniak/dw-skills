# --- Repo-specific (outside the dw-bootstrap managed block; survives re-runs) ---

# Node / pnpm (CI tooling: agnix, prettier)
node_modules/

# Scratch & personal notes (never published)
/tmp-*.md
/*.local.md
/research.md

# Vendored external dev tool (improve skill) — personal, not this repo's product
# (worktree scratch is in the managed block below)
/.agents/
/.claude/skills/
/skills-lock.json

# >>> dw-bootstrap managed block >>>
# Personal agent memory & local overrides — never committed.
/CLAUDE.local.md
/.claude/settings.local.json

# Worktree scratch — `claude --worktree` / `git worktree add` land here.
/.claude/worktrees/

# Editor / OS scratch.
.DS_Store
*.swp
.vscode/
.idea/
.obsidian/

# NOTE: `.ai/`, `.claude/settings.json`, and `.claude/hooks/` are TRACKED on
# purpose — dw-* treats agent infra (specs, plans, handoffs, guardrails) as
# shared, committed work. Do NOT add them here.
# <<< dw-bootstrap managed block <<<
